<Window x:Class="Lab07A2025.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Kenny Palacin" Height="680" Width="1200"
        WindowStartupLocation="CenterScreen">

    <Window.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#0F172A" Offset="0"/>
            <GradientStop Color="#111827" Offset="1"/>
        </LinearGradientBrush>
    </Window.Background>

    <Window.Resources>
        <!-- Paleta moderna azul/gris -->
        <SolidColorBrush x:Key="Primary" Color="#2563EB"/>
        <SolidColorBrush x:Key="PrimaryDark" Color="#1E40AF"/>
        <SolidColorBrush x:Key="Accent" Color="#38BDF8"/>
        <SolidColorBrush x:Key="CardBg" Color="#0B1220"/>
        <SolidColorBrush x:Key="Border" Color="#1F2937"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#E5E7EB"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#9CA3AF"/>

        <!-- TextBox moderno -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderBrush" Value="{StaticResource Border}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#0F172A"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" CornerRadius="8" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                Background="{TemplateBinding Background}">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="4,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource Primary}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Labels -->
        <Style x:Key="ModernLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>

        <!-- Botón primario -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource Primary}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="8" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryDark}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Botón secundario -->
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderBrush" Value="{StaticResource Border}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="8" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DataGrid moderno -->
        <Style x:Key="ModernDataGrid" TargetType="DataGrid">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowBackground" Value="#0F172A"/>
            <Setter Property="AlternatingRowBackground" Value="#111827"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="RowHeight" Value="40"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
        </Style>
        
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{StaticResource Primary}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
        </Style>
        
        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="#0F172A"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1E293B"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#374151"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
        </Style>
    </Window.Resources>

    <DockPanel>
        <!-- Header -->
        <Border DockPanel.Dock="Top" Background="{StaticResource CardBg}" 
                BorderBrush="{StaticResource Border}" BorderThickness="0,0,0,1" 
                Padding="20,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Título y búsqueda -->
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="📦 Gestión de Productos" 
                               Foreground="{StaticResource TextPrimary}" 
                               FontSize="22" FontWeight="Bold" 
                               VerticalAlignment="Center"/>
                    
                    <Rectangle Width="1" Height="24" Fill="{StaticResource Border}" 
                               Margin="20,0"/>
                    
                    <Border Background="{StaticResource Primary}" 
                            CornerRadius="8,0,0,8" Padding="12,8">
                        <TextBlock Text="🔍" FontSize="14"/>
                    </Border>
                    <TextBox x:Name="txtBuscador" Width="280" 
                             Style="{StaticResource ModernTextBox}" 
                             TextChanged="txtBuscador_TextChanged">
                        <TextBox.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="0,8,8,0"/>
                            </Style>
                        </TextBox.Resources>
                    </TextBox>
                    
                    <CheckBox x:Name="chkSoloActivos" Content="Solo activos" 
                              Foreground="{StaticResource TextSecondary}" 
                              VerticalAlignment="Center" Margin="16,0,0,0" 
                              Checked="chkSoloActivos_Checked" 
                              Unchecked="chkSoloActivos_Checked"/>
                </StackPanel>
                
                <!-- Botones de acción -->
                <StackPanel Orientation="Horizontal" Grid.Column="1">
                    <Button x:Name="btnNuevo" Content="Nuevo" 
                            Style="{StaticResource SecondaryButton}" 
                            Click="btnNuevo_Click" Margin="0,0,8,0"/>
                    <Button x:Name="btnGuardar" Content="Guardar" 
                            Style="{StaticResource PrimaryButton}" 
                            Click="btnGuardar_Click" Margin="0,0,8,0"/>
                    <Button x:Name="btnReactivar" Content="Reactivar" 
                            Style="{StaticResource SecondaryButton}" 
                            Click="btnReactivar_Click" Margin="0,0,8,0"/>
                    <Button x:Name="btnEliminar" Content="Eliminar" 
                            Style="{StaticResource SecondaryButton}" 
                            Click="btnEliminar_Click" IsEnabled="False"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Contenido principal -->
        <Grid Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="400"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Formulario -->
            <Border Background="{StaticResource CardBg}" CornerRadius="12" 
                    Padding="24" Grid.Column="0" 
                    BorderBrush="{StaticResource Border}" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Información del Producto" 
                               Foreground="{StaticResource TextPrimary}" 
                               FontSize="18" FontWeight="Bold" 
                               Margin="0,0,0,20"/>
                    
                    <!-- Nombre -->
                    <TextBlock Text="Nombre del producto" Style="{StaticResource ModernLabel}"/>
                    <TextBox x:Name="txtProdName" Style="{StaticResource ModernTextBox}" 
                             Margin="0,0,0,16"/>
                    
                    <!-- Precio -->
                    <TextBlock Text="Precio (S/)" Style="{StaticResource ModernLabel}"/>
                    <TextBox x:Name="txtPrice" Style="{StaticResource ModernTextBox}" 
                             PreviewTextInput="Numeric_PreviewTextInput" 
                             DataObject.Pasting="Numeric_Pasting"
                             Margin="0,0,0,16"/>
                    
                    <!-- Stock -->
                    <TextBlock Text="Stock disponible" Style="{StaticResource ModernLabel}"/>
                    <TextBox x:Name="txtStock" Style="{StaticResource ModernTextBox}" 
                             PreviewTextInput="Integer_PreviewTextInput" 
                             DataObject.Pasting="Integer_Pasting"
                             Margin="0,0,0,16"/>
                    
                    <!-- Estado activo -->
                    <CheckBox x:Name="chkActive" Content="Producto activo" 
                              Foreground="{StaticResource TextPrimary}" 
                              Margin="0,8,0,20"/>
                    
                    <!-- Estado del formulario -->
                    <TextBlock x:Name="lblStatus" 
                               Foreground="{StaticResource TextSecondary}" 
                               FontSize="12" Text="Completa la información"/>
                </StackPanel>
            </Border>

            <!-- Lista de productos -->
            <Border Grid.Column="2" Background="{StaticResource CardBg}" 
                    CornerRadius="12" Padding="16" 
                    BorderBrush="{StaticResource Border}" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Text="Lista de Productos" 
                               Foreground="{StaticResource TextPrimary}" 
                               FontSize="16" FontWeight="SemiBold" 
                               Margin="0,0,0,12"/>
                    
                    <DataGrid x:Name="dataGrid" Grid.Row="1"
                              Style="{StaticResource ModernDataGrid}" 
                              SelectionChanged="dataGrid_SelectionChanged">
                        <DataGrid.Columns>
                            <DataGridTextColumn Binding="{Binding ProductId}" 
                                                Header="ID" Width="60" IsReadOnly="True"/>
                            <DataGridTextColumn Binding="{Binding Name}" 
                                                Header="Producto" Width="*" IsReadOnly="True"/>
                            <DataGridTextColumn Binding="{Binding Price, StringFormat=C2}" 
                                                Header="Precio" Width="120" IsReadOnly="True"/>
                            <DataGridTextColumn Binding="{Binding Stock}" 
                                                Header="Stock" Width="80" IsReadOnly="True"/>
                            <DataGridCheckBoxColumn Binding="{Binding Active}" 
                                                    Header="Activo" Width="80" IsReadOnly="True"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>
    </DockPanel>
</Window>